import 'package:flutter/cupertino.dart';

class MessageDriver extends StatelessWidget {
  const MessageDriver({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
